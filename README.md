# Install

Add to `compose.json` the following:

```
  "repositories": [
    {
      "type": "path",
      "url": "../craft-plugin-generate-pdf"
    }
  ],
```

Install via:

```
composer require spg/generate-pdf
```
